import React, {FC} from 'react';
import {Typography} from 'antd';

const {Text} = Typography;

export const Home: FC = () => {
    return (
        <div>
            <Text>Home</Text>
        </div>
    )
}